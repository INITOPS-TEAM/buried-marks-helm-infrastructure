apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "map-service.fullname" . }}-test-connection"
  labels:
    {{- include "map-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "map-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
