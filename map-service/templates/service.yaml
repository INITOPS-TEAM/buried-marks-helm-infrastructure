apiVersion: v1
kind: Service
metadata:
  name: {{ include "map-service.fullname" . }}
  labels:
    {{- include "map-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "map-service.selectorLabels" . | nindent 4 }}
